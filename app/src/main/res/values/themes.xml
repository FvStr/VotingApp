<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AplikasiVoting" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/biru</item>
        <item name="colorPrimaryVariant">@color/biru</item>
        <item name="android:statusBarColor">@color/biru</item>
    </style>

    <style name="Theme.AplikasiVoting" parent="Base.Theme.AplikasiVoting" />

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/birumuda</item>
    </style>
</resources>